package com.appsforprogress.android.mycareerpath;

import android.support.v4.app.Fragment;

/**
 * Created by Oswald on 1/10/2016.
 */
public class AttributeListActivity //extends SingleFragmentActivity
{
    /*
    // Add an Extra to hold the SkillList item selected in the SkillScrollerActivity
    private static final String EXTRA_SKILL_ID = "com.appsforprogress.android.mycareerpath.skill_id";

    // Communicate with the SkillScrollerActivity about the item to be displayed
    public static Intent newIntent(Context packageContext, UUID skillId)
    {
        // Intent(HASH)
        Intent intent = new Intent(packageContext, SkillListActivity.class);

        // Insert an extra key, value pair into the hash
        // This extra is sent to the SkillFragment which skill to display
        intent.putExtra(EXTRA_SKILL_ID, skillId);

        return intent;
    }

    @Override
    // Describe the Fragment to be implemented
    protected Fragment createFragment()
    {
        return AttributeListFragment.newInstance();
    }
    */
}
